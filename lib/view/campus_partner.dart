import 'package:flutter/material.dart';

class CampusPartner extends StatelessWidget {
  const CampusPartner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
